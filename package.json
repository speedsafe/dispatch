{
  "name": "speedsafe-dispatch",
  "version": "1.0.0",
  "description": "SpeedSafe Dispatch PWA - GPS tracking and push notifications for field technicians",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "NODE_ENV=development node server.js",
    "build": "echo 'Building SpeedSafe Dispatch PWA' && echo 'index.html service-worker.js manifest.json speedsafe-dispatch.css speedsafe-dispatch-app.jsx' > /dev/null",
    "test": "echo 'Run manual tests at http://localhost:5000'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "socket.io": "^4.5.4",
    "@supabase/supabase-js": "^2.38.0",
    "axios": "^1.6.0",
    "web-push": "^3.6.6",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=16.0.0"
  }
}
